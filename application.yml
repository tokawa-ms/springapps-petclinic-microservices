spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tokawa-ms/springapps-petclinic-microservices-config
        native:
          search-locations: classpath:.
  profiles:
    active: native
